package game.world.creatures;

/**
 * Used to define what a creature is currently doing, so that if a creature is trying to eat/mate/etc.
 */
public enum CreatureState {
	MATING, WANDERING, EATING, CHASING, FLEEING
}
